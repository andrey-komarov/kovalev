#include <fstream>
#include <string>
#include "long.h"

int main()
{
	std::ifstream in("in.txt");
	std::ofstream out("out.txt");
	longint a, b;
	in >> a >> b;
	//~ longint c = (a + b + 2) / 2;
	//~ out << c;
	out << (a / b);
}
